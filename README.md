A folder has been created for each kata.

Some katas has unit tests, in order to execute them, [Composer](https://getcomposer.org/download/) must be installed and then install the dependencies with:
`composer install`

